import React from 'react';

const CategoriesDetailsPge = ({params}) => {
    console.log({params});
    return (
        <div>
            categories detail page
        </div>
    );
};

export default CategoriesDetailsPge;